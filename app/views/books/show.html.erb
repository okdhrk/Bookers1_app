<div class="font-flash">
    <%= flash[:notice] %>
</div>

<div class="box2">
    Title: <%= @book.title %><br>
    Body: <%= @book.body %>
</div>

<%= link_to "Edit", edit_book_path(@book) %>
|
<%= link_to "Back", books_path(@book) %>